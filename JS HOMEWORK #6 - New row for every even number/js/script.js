for (let num = 1; num <= 20; num++) {
    if (num % 2 === 1) {
        console.log(num + ` ` + `${num+1}` /*+ `\n` */ ); //Ne gledam pricina zosto  da stoi i toa console.log ja vrsi istata funkcija (\n)
    }
}